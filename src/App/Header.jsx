import React from 'react';

const Header = () => (
    <header>
        <nav>
            <ul>
                <li> Hola </li>
            </ul>
        </nav>
    </header>
);

export default Header;
